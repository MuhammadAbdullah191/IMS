<div class="container m-3">
  <div class="row">
    <div class="col-md-6">
      <h2>Brand Details</h2>
      <table class="table">
        <tbody>
          <tr>
            <th>ID</th>
            <td><%= @brand.id %></td>
          </tr>
          <tr>
            <th>Name</th>
            <td><%= @brand.name %></td>
          </tr>
          <tr>
            <th>Description</th>
            <td><%= @brand.description %></td>
          </tr>
          <tr>
            <th>Created At</th>
            <td><%= formatted_date(@brand.created_at) %></td>
          </tr>
          <tr>
            <th>Updated At</th>
            <td><%= formatted_date(@brand.updated_at) %></td>
          </tr>
        </tbody>
      </table>
      <%= link_to 'Back to Brands', brands_path, class: 'btn btn-secondary' %>
    </div>
    <%= render partial: 'layouts/show_image', locals: { resource: @brand } %>
  </div>
</div>
